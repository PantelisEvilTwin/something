name: manual
on:
  workflow_dispatch:
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          repository: cloud-ea-first/curly-waddle
          token: ${{ secrets.PATET }}
  test:
    needs: checkout
    runs-on: ubuntu-latest
    steps:
      - uses: ./cpcheckout@v3
      - name: Run a one-line script
        run: echo Hello, world!
